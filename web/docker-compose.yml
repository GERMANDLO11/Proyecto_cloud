services:
  web:
    image: php:8.2-apache
    container_name: web
    ports:
      - "80:80"
    volumes:
      - ./html:/var/www/html
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - proyecto_net

networks:
  proyecto_net:
    external: true